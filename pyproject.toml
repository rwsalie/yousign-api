[project]
name = "yousign3"
version = "0.0.6"
description = "a complete and safe use of YouSign V3 API"
authors = [
    {name = "rwsalie",email = "rwsalie@iscarioth.org"}
]
license = {text = "ISC"}
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic[email] (>=2.11.7,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
